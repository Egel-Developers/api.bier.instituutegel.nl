{
  "name": "bier-server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "deploy": "docker buildx build --platform linux/arm64/v8,linux/amd64 --push -t instituutegel/bier-server ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
